{
  "id":"123",
  "variable": "${variable}",
  "student":{
    "age": "${student.age}",
    "name": "${student.name}"
  },
  "myMap": {
    "mapKey": "${map.key}",
    "allKeys": {
      <#list map?keys as key>
        ${key}
      </#list>

    }
  },
  "myList": {
    "myList-1": "${list[0]}",
    "myList-2": "${list[1]}",
    "size": "${list?size}"
  },
  "null-value": {
    "null": "${nullValue!'默认值'}",
    "notNull": "${notNullValue!aaa}"
  }


}